<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.LechWidgets.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:gravity="center"
    android:soundEffectsEnabled="true"
    android:animateLayoutChanges="true"
    android:orientation="vertical"
    android:clipToOutline="true"
    android:background="@drawable/round_corner"
    android:theme="@style/Theme.LechWidgets.AppWidgetContainer">
    <ImageView
        android:id="@+id/initialImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:src="@drawable/lech_pinterest_initial"
        android:contentDescription="@string/lech_pinterest_description" />

    <TextView
        android:id="@+id/reloadWidget"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#B3000000"
        android:gravity="center"
        android:padding="10.dp"
        android:shadowColor="#FFFFFF"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="10"
        android:text="@string/pinterest_reloadMessage"
        android:textAlignment="center"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold|italic" />
</RelativeLayout>